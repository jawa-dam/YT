(()=>{"use strict";const DAYS=6,storeKey="geiDayCompletionV1",pathFor=(n)=>`day-${n}.html`;const read=()=>{try{return JSON.parse(localStorage.getItem(storeKey)||"{}")}catch{return{}}};function apply(){const done=read();document.querySelectorAll("#screen-academy .academy-day-card").forEach(card=>{const day=Number(card.dataset.day);if(!day)return;const unlocked=day===1||!!done[day-1];const complete=!!done[day];card.href=pathFor(day);card.target="_self";card.rel="";card.classList.toggle("is-locked",!unlocked);card.classList.toggle("is-complete",complete);card.setAttribute("aria-disabled",String(!unlocked));const status=card.querySelector(".academy-day-status");if(status)status.textContent=complete?"COMPLETED":unlocked?(day===1?"START HERE":"UNLOCKED"):"LOCKED";if(!unlocked)card.dataset.geiLocked="true";else delete card.dataset.geiLocked});const begin=document.querySelector("#screen-academy .academy-primary-action");if(begin){begin.href=pathFor(1);begin.target="_self";begin.rel=""}const guide=document.querySelector("#screen-academy .academy-guide-action");if(guide){guide.href=pathFor(1);guide.target="_self";guide.rel=""} }function boot(){apply();document.addEventListener("click",e=>{const card=e.target.closest?.("#screen-academy .academy-day-card[data-gei-locked='true']");if(!card)return;e.preventDefault();e.stopPropagation();const day=Number(card.dataset.day);window.dispatchEvent(new CustomEvent("gei:day-locked",{detail:{day}}))},true);window.addEventListener("storage",apply);window.addEventListener("gei:day-completion",apply);window.setTimeout(apply,50);window.setInterval(apply,1000)}if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",boot,{once:true});else boot()})();