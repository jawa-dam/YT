/* V1.32 — Adam Mastery Celebration */
(() => {
  "use strict";
  const MASTERY_KEY="geiAdamObjectiveMasteryV1", CELEBRATION_KEY="geiAdamMasteryCelebrationsV1";
  const DAYS=Object.freeze([1,2,3,4,5,6]), PER_DAY=3;
  function loadMastery(){try{const d=JSON.parse(localStorage.getItem(MASTERY_KEY)||"{}");return Array.isArray(d.mastered)?[...new Set(d.mastered.map(String))]:[]}catch(_){return[]}}
  function loadState(){try{const d=JSON.parse(localStorage.getItem(CELEBRATION_KEY)||"{}");return{days:Array.isArray(d.days)?[...new Set(d.days.map(Number).filter(x=>DAYS.includes(x)))]:[],allSix:d.allSix===true}}catch(_){return{days:[],allSix:false}}}
  function save(s){try{localStorage.setItem(CELEBRATION_KEY,JSON.stringify(s))}catch(_) {}}
  function count(m,day){return m.filter(k=>k.startsWith(`${day}-`)).length}
  function sync(m){const s=loadState();DAYS.forEach(d=>{if(count(m,d)>=PER_DAY&&!s.days.includes(d))s.days.push(d)});s.days.sort((a,b)=>a-b);if(s.days.length===6)s.allSix=true;save(s);return s}
  function build(){const e=document.createElement("section");e.className="adam-mastery-celebration";e.setAttribute("aria-labelledby","adam-celebration-title");e.innerHTML=`<div class="adam-celebration-main"><span class="adam-celebration-icon" aria-hidden="true">✓</span><div class="adam-celebration-copy"><span class="adam-celebration-kicker">ADAM • MASTERY ACHIEVEMENT</span><h3 id="adam-celebration-title">Mastery milestones</h3><p id="adam-celebration-message" aria-live="polite"></p></div></div><div class="adam-celebration-status" id="adam-celebration-status" aria-label="Mastery achievement status"></div>`;return e}
  function render(){const academy=document.getElementById("screen-academy"),view=academy?.querySelector(".academy-view"),mastery=view?.querySelector(":scope > .adam-objective-mastery");if(!view||!mastery)return;let s=view.querySelector(":scope > .adam-mastery-celebration");if(!s){s=build();view.insertBefore(s,mastery.nextElementSibling||null)}const m=loadMastery(),st=sync(m),message=s.querySelector("#adam-celebration-message"),status=s.querySelector("#adam-celebration-status"),active=Number(window.GEI_PROGRESS?.getCurrentDay?.())||1;if(message)message.textContent=st.allSix?"All six days mastered — 18 of 18 checkpoints demonstrated.":st.days.length?`Day ${st.days[st.days.length-1]} mastered. Keep building the blueprint.`:`Complete Day ${active}'s 3 checkpoints to earn its mastery recognition.`;if(status)status.innerHTML=DAYS.map(d=>{const done=st.days.includes(d),n=count(m,d);return `<span class="adam-celebration-pill${done?" is-complete":""}" aria-label="Day ${d}: ${done?"mastered":`${n} of 3 checkpoints`}"><b>D${d}</b><small>${done?"✓":`${n}/3`}</small></span>`}).join("");s.classList.toggle("is-all-complete",st.allSix)}
  function init(){render();document.addEventListener("gei:navigation",render);document.addEventListener("gei:progress-ready",render);document.addEventListener("gei:progress-updated",render);window.addEventListener("storage",e=>{if(e.key===MASTERY_KEY||e.key===CELEBRATION_KEY)render()});window.setInterval(render,900)}
  if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",init,{once:true});else init();
})();